# Web server + real-time dashboard
Flask==3.0.3
Flask-SocketIO==5.3.6
python-socketio==5.11.4
python-engineio==4.9.1

# Production-capable async server for SocketIO
eventlet==0.36.1

# Metric collection (host/container resource sampling)
psutil==6.0.0

# Numeric / prediction (load forecasting)
numpy==2.1.1

# Config
PyYAML==6.0.2

# Testing
pytest==8.3.3
pytest-cov==5.0.0

# E2E / load testing (client)
# requests: HTTP client for the black-box verifier + load test.
# websocket-client: gives socketio.SimpleClient a real WebSocket transport
# (python-socketio / python-engineio are already pinned above).
requests==2.32.3
websocket-client==1.8.0

# Container orchestration (opt-in Docker worker backend)
# Only used when USE_DOCKER=1 selects the DockerWorkerPool; imported lazily so the
# default simulated path never needs the SDK or a running daemon.
docker==7.1.0
