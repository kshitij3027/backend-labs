# Byte-compiled / optimized
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# Environment / secrets
.env

# Test & tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Generated storage data & logs (the engine writes these at runtime).
# Anchored with a leading slash so they only match the project-root dirs and
# never the `src/storage/` source package.
/data/
/logs/
/storage/
*.parquet
*.arrow
*.db
*.sqlite
*.sqlite3

# OS
.DS_Store

# Planning doc (repo convention — keep local, do not commit)
plan.md
project_requirements.md
