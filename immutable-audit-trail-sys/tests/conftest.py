"""Shared pytest fixtures for the immutable-audit-trail-sys test suite.

Empty in C0 — fixtures land here as subsequent commits introduce real
state (DB engine, signing key, app client). Kept as a stub now so test
modules added in C1+ can import from it without race conditions.
"""
