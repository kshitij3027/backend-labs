# --- Web framework / server ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Data validation / config ---
pydantic==2.9.2
pydantic-settings==2.5.2

# --- Cryptography (Ed25519 signing; SHA-256 from stdlib hashlib) ---
cryptography==43.0.1

# --- Persistence (append-only audit_records table) ---
sqlalchemy==2.0.35
aiosqlite==0.20.0

# --- Dashboard templating ---
jinja2==3.1.4

# --- Form-encoded request parsing (FastAPI Form()) ---
python-multipart==0.0.12

# --- Observability ---
prometheus-client==0.20.0
prometheus_fastapi_instrumentator==7.0.0

# --- Config / env ---
python-dotenv==1.0.1
pyyaml==6.0.2

# --- HTTP client (used in tests and the e2e script) ---
httpx==0.27.2

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
asgi-lifespan==2.1.0
pytest-cov==5.0.0
