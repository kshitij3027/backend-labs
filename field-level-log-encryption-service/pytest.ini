[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -v --tb=short --strict-markers
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: tests that require live external services (e.g. Redis)
