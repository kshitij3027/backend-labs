services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    command: ["serve", "--host", "0.0.0.0", "--port", "8080"]
    volumes:
      - ./logs:/app/logs
      - ./output:/app/output
    ports:
      - "8080:8080"

  test:
    build:
      context: .
      dockerfile: Dockerfile.test
    volumes:
      - ./logs:/app/logs
      - ./tests:/app/tests
      - ./src:/app/src
