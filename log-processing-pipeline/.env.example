# Override the path to config.yml (default: /app/config.yml inside Docker)
# CONFIG_PATH=/app/config.yml

# Override individual settings via env vars (component-specific, not yet wired)
# GENERATOR_RATE=10
# COLLECTOR_POLL_INTERVAL=2
