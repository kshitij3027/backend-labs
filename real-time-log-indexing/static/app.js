// Placeholder dashboard script — real logic (1 Hz stats polling,
// debounced search, filter dropdowns, WebSocket live feed) lands in
// Commit 10. The file exists now so the template can reference
// /static/app.js today without a 404 and so the Dockerfile has the
// asset baked into the image.

console.log("dashboard placeholder loaded");
