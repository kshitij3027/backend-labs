"""HTTP API package for the real-time log indexing engine.

Route handlers for the dashboard, search, stats, health, sample
generation, and the WebSocket endpoint live here. Submodules are
imported by ``src.main`` and mounted via ``app.include_router``.
"""
