"""HTTP routers for the log search service.

Routers are split by resource so ``main.py`` can include them
independently and tests can exercise one surface without dragging in
every other handler's dependencies.
"""
