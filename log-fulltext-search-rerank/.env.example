# Example environment for log-fulltext-search-rerank.
# Copy to `.env` (kept out of git) and adjust as needed.

# HTTP server bind address.
HTTP_HOST=0.0.0.0
HTTP_PORT=8000

# Log verbosity for the uvicorn/app logger.
LOG_LEVEL=INFO
