{
  "name": "rca-dashboard",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Live dashboard for the RCA Analysis Engine — React + Vite, served by nginx. Plotly (C12 causal graph) loads from CDN, not as an npm dep.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.3",
    "vite": "^5.4.10"
  }
}
