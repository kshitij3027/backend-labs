# Web framework / API server
fastapi==0.115.0
uvicorn[standard]==0.30.6

# WebSocket support (bundled via uvicorn[standard], listed explicitly for clarity)
websockets==13.0.1

# Data validation / models + settings
pydantic==2.9.2
pydantic-settings==2.7.1

# Causal graph construction & root-cause ranking
networkx==3.3

# Numerics / scoring / stats (anomaly lift, PageRank, vectorized power iteration)
numpy==1.26.4
scipy==1.13.0

# Confidence calibration (C9): isotonic / Platt regression + Brier score + reliability
# curve. Pinned to a build compatible with numpy 1.26 / scipy 1.13.
scikit-learn==1.5.2

# Tolerant timestamp parsing (ISO-8601 / offsets / clock-skew handling)
python-dateutil==2.9.0.post0

# Structured logging
structlog==24.4.0
python-json-logger==2.0.7

# Config / environment
python-dotenv==1.0.1

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
