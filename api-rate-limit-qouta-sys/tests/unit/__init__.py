# Package marker only. Unit tests: pure functions, settings parsing and the HTTP surface against
# an injected Runtime. Anything that needs a real Redis (the Lua scripts, the gateway, the
# distributed double-spend check) lives in tests/integration/ instead.
