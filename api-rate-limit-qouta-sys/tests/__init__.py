# Package marker only. Makes `tests.unit` / `tests.integration` importable as packages so two
# test modules may share a basename across the two trees without pytest's rootdir-relative module
# naming colliding.
