[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -ra --strict-markers --strict-config -m "not load"
filterwarnings =
    error
    ignore::DeprecationWarning
markers =
    integration: end-to-end tests requiring the full Docker stack
    load: long-running load/overhead measurements
