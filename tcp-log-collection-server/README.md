# TCP Log Collection Server

A TCP server that listens for incoming network connections and receives log data from remote clients. Runs as a long-lived server process — starts up, binds to a host/port, and continuously accepts client connections until manually stopped.

## Tech Stack
- Language: Python

## How to Run
<!-- Fill in as development progresses -->

## What I Learned
<!-- Fill in as the project evolves -->
